<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <title>Welcome</title>
</head>

<body class="bg-gradient-to-r from-blue-100 to-green-100 min-h-screen flex items-center justify-center">
  <div class="bg-white rounded-xl shadow-lg overflow-hidden max-w-2xl w-full">
    <header class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6">
      <div class="text-center">
        <h1 class="text-2xl font-bold">User Management System</h1>
      </div>
    </header>

    <!-- Flash Message Section -->
    <% if (messages.success && messages.length > 0) { %>
      <div class="bg-green-500 text-white p-4 rounded-lg mt-4 text-center">
        <%= messages.success %>
      </div>
    <% } %>

    <% if (messages.error && messages.length > 0) { %>
      <div class="bg-red-500 text-white p-4 rounded-lg mt-4 text-center">
        <%= messages.error %>
      </div>
    <% } %>

    <main class="p-8 text-center">
      <h2 class="text-3xl font-bold text-gray-800">Welcome to User Management System</h2>
      <p class="text-lg text-gray-600 mt-4">Manage users effectively with ease.</p>
      <div class="mt-8 flex justify-center space-x-4">
        <a href="/login" class="bg-blue-600 text-white px-8 py-3 rounded-lg shadow-md hover:bg-blue-500 transition-all">Login</a>
        <a href="/register" class="bg-green-600 text-white px-8 py-3 rounded-lg shadow-md hover:bg-green-500 transition-all">Register</a>
      </div>
    </main>

    <footer class="bg-gray-800 text-white text-center py-4">
      <p>© 2025 User Management System. All Rights Reserved.</p>
    </footer>
  </div>
</body>

</html>
